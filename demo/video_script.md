# FigureFlow Demo 录屏与讲解稿

> 建议时长：56 秒录屏 + 约 3 分钟现场讲解。仓库视频是固定离线预设复演，在线模式必须在配置真实 API Key 后现场运行，二者不要混称。

## 56 秒录屏旁白

| 时间 | 画面 | 旁白 |
|---|---|---|
| 00:00–00:06 | 输入业务描述与运行模式 | 日常汇报、SOP、论文与专利配图，常见做法要么是在企业微信或飞书里反复拉线、对齐，要么先生成整图，再反复修文字和箭头。前者慢，后者不稳定。 |
| 00:06–00:11 | 点击生成，界面显示运行中 | 这段仓库视频使用固定离线预设，方便无网络时稳定答辩；它不会伪装成一次 GPT-5.6-sol 实时调用。 |
| 00:11–00:18 | FigurePlan 与阶段耗时 | 在线模式下，GPT-5.6-sol 只把需求整理为受约束的 FigurePlan；这次画面记录的是同一条后续流水线的单机耗时，不是团队提效结论。 |
| 00:18–00:25 | 三套布局候选 | 相同语义可以进入三套确定性布局。模型不能直接写代码或任意坐标，布局、素材键和证据状态都经过白名单与结构校验。 |
| 00:25–00:32 | 原始 Chroma 素材 | 生成式视觉能力只生产无文字、可替换的局部 Icon，从源头避免整图里的伪文字、错公式和关系漂移。 |
| 00:32–00:39 | 透明抠图前后对照 | 自动化脚本完成 Chroma key、soft matte、去色边和裁切，并记录输入、输出、参数与文件哈希。 |
| 00:39–00:48 | 最终精美流程图 | 中文、箭头、证据状态、图注和质量门由确定性程序排版，所以文字准确、连线可控，也能只替换一个节点而不重做整张图。 |
| 00:48–00:56 | SVG / PDF / ZIP / Manifest 下载 | 最后一次导出 SVG、PDF、PNG、Manifest 和交付 ZIP。真正的提效比例会用手工拉线、整图生成后二次编辑和 FigureFlow 做同任务对照后再报告。 |

## 现场 3 分钟讲解结构

1. **问题（30 秒）**：说明两条 baseline——文档内手工拉线、整图生成后二次编辑——各自的重复劳动与质量风险。
2. **为什么适合 AI（30 秒）**：需求理解、信息分组和视觉隐喻需要语义判断；但精确文字、公式、箭头和版式不应交给概率式生图模型。
3. **Demo（70 秒）**：展示输入、FigurePlan、布局候选、无文字 Icon、透明抠图、确定性排版、自动 QA 与多格式导出。
4. **工程边界（25 秒）**：在线模式才会调用 GPT-5.6-sol；离线视频是固定预设复演。公开演示默认禁用实时生图，避免泄密、费用滥用与磁盘堆积。
5. **量化方案（25 秒）**：用同一任务和同一验收表比较三种方法，记录首个合格版本耗时、人工操作时间、局部修改耗时、文字/关系错误和可编辑交付率。

## 答辩现场切换建议

- 网络和 API Key 都正常：先现场跑 `online`，再用仓库视频兜底展示完整交付链。
- 无法访问外网：直接播放离线视频，并明确说“语义规划是固定预设，素材处理、排版、QA 和导出是真实执行”。
- 评委追问提效数字：只报告已经录入 `benchmark/results_template.csv` 并通过统计脚本校验的实测数据；没有实测时只展示本次流水线机器耗时。
